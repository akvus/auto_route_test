import 'package:flutter/material.dart';

class Child1Page extends StatelessWidget {
  const Child1Page({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Child 1'));
  }
}
