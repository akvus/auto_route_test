import 'package:flutter/material.dart';

class Child21Page extends StatelessWidget {
  const Child21Page({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Child 2_1'));
  }
}
